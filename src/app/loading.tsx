import { KamooBrandLoader } from "@/components/kamoo/loading";

/**
 * Écran de chargement RACINE — boot de l'app / transitions de haut niveau.
 * Plein écran de marque (logo qui respire + liseré indéterminé).
 */
export default function Loading() {
  return <KamooBrandLoader />;
}
